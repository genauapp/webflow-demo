export const LEARN_ELEMENT_IDS = (type) => {
  return [
    `addToFavoritesLearn-${type}`,
    `wordLearn-${type}`,
    `translationLearn-${type}`,
    `ruleLearn-${type}`,
    `sentenceHead-${type}`,
    `exampleLearn-${type}`,
  ]
}
